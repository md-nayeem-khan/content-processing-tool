% Section content template for individual Educative lessons
% This file contains the actual content for a specific section
% Generated from Educative API section content

% Section: System Design: Web Crawler
% Section ID: 4695113376989184
% Section Slug: system-design-web-crawler
% Generated: 2025-12-08T13:47:24.417922

\subsection{Introduction}\label{S7jXFe80tYndqj-0wwhlE}
\\
A \textbf{web crawler} is an internet bot that systematically scours(To go or move swiftly about, over, or through in search of something.) the World Wide Web (WWW) for content, starting its operation from a pool of seed URLs. This process of acquiring content from the WWW is called~the crawling process. The crawler further saves the content in data stores, ensuring the data is available for later use. Efficient storage and subsequent retrieval of this data are integral to designing a robust system.
\\
The core functionality of a web crawler involves fetching web pages, parsing their content and metadata, and extracting new URLs or lists of URLs for further crawling. This is the first step performed by search engines. The output of the crawling process serves as input for subsequent stages such as:

\begin{itemize}
\item
\protect\phantomsection\label{n-ma5kx29dzHOjLjcEz4W}
Data cleaning
\item
\protect\phantomsection\label{d0eeGRL2E-e6YqML2nZrV}
Indexing
\item
\protect\phantomsection\label{eezIXGbCFJu6sS8WPi7Rn}
Relevance scoring using algorithms like PageRank
\item
\protect\phantomsection\label{2WmEGGXxo9-KvUy88AcOF}
URL frontier management
\item
\protect\phantomsection\label{kTmutk9s9KAAAfoNNc3Kq}
Analytics
\end{itemize}
\\
This specific design problem is focused on web crawlers' System Design and excludes explanations of the later stages of indexing, ranking in search engines, etc. To learn about some of these subsequent stages, refer to our chapter on \href{https://www.educative.io/courses/grokking-the-system-design-interview/system-design-the-distributed-search}{distributed search}.

\begin{figure}[htbp]
 \centering
 \includegraphics[width=0.5\textwidth]{Images/chapter_35/section_4695113376989184/6133375607635968.png}
 \caption{An overview of the web crawler system}
\end{figure}

\subsubsection{Benefits of a Web Crawler}\label{j30JnLuyJ-T-0OzB0IfZS}
\\
Web crawlers offer various utilities beyond data collection:

\begin{itemize}
\item
\protect\phantomsection\label{ST4iL6Weapz_kk-Le5lR8}
\textbf{Web page testing}: Web crawlers test the validity of the links and structures of HTML pages.
\item
\protect\phantomsection\label{ir1OXFSPzLtlSReKVRcNy}
\textbf{Web page monitoring}: We use web crawlers to monitor the content or structure updates on web pages.
\item
\protect\phantomsection\label{gLDjT0rcrPXb8brp-w-q1}
\textbf{Site mirroring}: Web crawlers are an effective way to mirror(Mirroring is like making a dynamic carbon copy of a website. Mirroring refers to network services available by any protocol, such as HTTP or FTP. The URLs of these sites differ from the original sites, but the content is similar or almost identical.) popular websites.
\item
\protect\phantomsection\label{S8mZIUKUpW9tGUByddXIA}
\textbf{Copyright infringement check}: Web crawlers fetch and parse page content and check for copyright infringement issues.
\end{itemize}

\subsubsection{Challenges of a Web Crawler System Design}\label{FOuwuNm4bjU-YDSspBn4s}
\\
While designing a web crawler, several challenges arise:

\begin{itemize}
\item
\protect\phantomsection\label{mPr-op1GKOdkM7peUAAP-}
\textbf{Crawler traps:} Infinite loops caused by dynamic links or calendar pages.
\item
\protect\phantomsection\label{BYpBu4HhC8AWMcaVlWhTo}
\textbf{Duplicate content:} Crawling the same web pages repeatedly wastes resources.
\item
\protect\phantomsection\label{-6ORkA8SLNL0UId1MaVCM}
\textbf{Rate limiting:} Fetching too many pages from a single domain can lead to server overload. We need load balancing to balance the loads on web servers or application servers.
\item
\protect\phantomsection\label{7CNrjgX6qc_W6TjX_PEOb}
\textbf{DNS lookup latency:} Frequent domain name system (DNS) lookups increase latency.
\item
\protect\phantomsection\label{IhxdB1Tvs8Gaef8OCmNLE}
\textbf{Scalability}: Handling large-scale crawling is challenging and demands a distributed system that can process millions of seed URLs and distribute load across multiple web servers.
\end{itemize}
\\
Designing a web crawler is a common System Design interview question to test candidates' understanding of components like HTML fetcher, extractor, scheduler, etc. The interviewer can ask the following interesting questions:

\begin{itemize}
\item
How would you design a web crawler system that can handle large datasets, and how would you incorporate Redis for caching and Amazon Web Services (AWS) for scalability?
\item
How would you handle request timeouts and manage rate limits set by websites?
\item
What optimization strategies would you use for components like parser, fetcher, etc., for large-scale use cases like those at FAANG?
\item
How metrics like response time, cache hit rate, etc., help evaluate web crawlers' performance to crawl large datasets for aggregation.
\end{itemize}
\\
Let's begin by defining the requirements of a web crawler system.

\subsection{Requirements}\label{YT6t3M-yrq31Rs76iqETT}
\\
Let's highlight the functional and non-functional requirements of a web crawler.

\subsubsection{Functional requirements}\label{8kx7lYyQiwqvqW9BGINMS}
\\
These are the functionalities a user must be able to perform:

\begin{itemize}
\item
\protect\phantomsection\label{iVlcPpoq9Tai0r3cr3CO-}
\textbf{Crawling}: The system should scour the WWW, spanning from a queue of seed URLs provided initially by the system administrator.
\end{itemize}

\begin{quote}
\textbf{Quiz:}
\textbf{Question 1:} Where do we get these seed URLs from?
\textbf{Answer:} There are two possible ways to create or gather seed URLs: 1. We can manually create them 2. We can scan the IP addresses for the presence of web servers These seed URLs must be of good quality.
\textbf{Question 2:} Why are good quality seed URLs important and what happens if the seed quality is not good?
\textbf{Answer:} When we model WWW as a graph where URLs link one node to another, our goal is to discover as much of it as possible during the crawl. Using a low-quality seed might limit the discovery to just a fraction of the WWW

graph.
\textbf{Question 3:} How do we select seed URLs for crawling?
\textbf{Answer:} There are multiple approaches to selecting seed URLs. Some of them are:

- Location-based: We can have different seed URLs depending on the location of the crawler. - Category-based: Depending on the type of content we need to crawl, we can have various sets of seed URLs. - Popularity-based: This is the most popular approach. It combines both the aforementioned approaches. It groups the seed URLs based on hot topics in a specific area.
\end{quote}

\begin{itemize}
\item
\protect\phantomsection\label{9LzLExTV2e6hbte7zBnUe}
\textbf{Storing}: The system should be able to extract and store the content of a URL in a blob store. This makes that URL and its content processable by the search engines for indexing and ranking purposes.
\item
\protect\phantomsection\label{9rHTsCtEwnRm5X_22TGVl}
\textbf{Scheduling}: Since crawling is a process that's repeated, the system should have regular scheduling to update its blob stores' records.
\end{itemize}

\subsubsection{Non-functional requirements}\label{GqWa2BqUX3ML-wYuBFpXc}

\begin{itemize}
\item
\protect\phantomsection\label{IkE2XouSmsLJKXP6MsGOZ}
\textbf{Scalability}: The system should inherently be distributed and multithreaded, because it has to fetch hundreds of millions of web documents.
\item
\protect\phantomsection\label{sIUyVieKzQJur8yg8vS-O}
\textbf{Extensibility}: Currently, our design supports HTTP(S) communication protocol and text files storage facilities. For augmented functionality, it should also be extensible for different network communication protocols, able to add multiple modules to process, and store various file formats.
\item
\protect\phantomsection\label{BycBpeKpoAlvB8Cp7EkOt}
\textbf{Consistency}: Since our system involves multiple crawling workers, having data consistency among all of them is necessary.\\
In the general context, data consistency means the reliability and accuracy of data across a system or dataset. In the web crawler's context, it refers to the adherence of all the workers to a specific set of rules in their attempt to generate consistent crawled data.
\item
\protect\phantomsection\label{qL1dAhhnntylRWYurOro7}
\textbf{Performance}: The system should be smart enough to limit its crawling to a domain, either by time spent or by the count of the visited URLs of that domain. This process is called \textbf{self-throttling}. The URLs crawled per second and the throughput of the content crawled should be optimal.
\end{itemize}

Websites usually host a \texttt{robots.txt} file, which communicates domain-specified limitations to the crawler. The crawler should adhere to these limitations by all means.

\begin{itemize}
\item
\protect\phantomsection\label{2LWT05HnoZW2T0hoMe9Eb}
\textbf{Improved user interface---customized scheduling}: Besides the default recrawling, which is a functional requirement, the system should also support the functionality to perform non-routine customized crawling on the system administrator's demands.
\end{itemize}

\begin{figure}[htbp]
 \centering
 \includegraphics[width=0.25\textwidth]{Images/chapter_35/section_4695113376989184/6030943022481408.png}
 \caption{The non-functional requirements of the web crawler system}
\end{figure}

With our requirements established, we can now quantify the immense scale our system must handle.

\subsection{Resource estimation}\label{Ix0bnfM4Rka7mmFolY2E1}
\\
We need to estimate various resource requirements for our design.
\\
\textbf{Assumptions}
\\
These are the assumptions we'll use when estimating our resource requirements:

\begin{itemize}
\item
\protect\phantomsection\label{zjNFx4YUIN82LpH5-QKVw}
There are a total of 5 billion web pages.
\item
\protect\phantomsection\label{FDDKpiF64SpP9eMwCYBAx}
The text content per webpage is 2070 KB(A study suggests that the average size of a webpage content is 2070KB (2.07MB) based on 892 processed websites.).
\item
\protect\phantomsection\label{SB5uKsydUCD2gCBLGKi6P}
The metadata(It consists of a webpage title and description of the web page showing its purpose.) for one web page is 500 Bytes.
\end{itemize}

\subsubsection{Storage estimation}\label{ulrO5ZYhDxKH8_ucMpDPv}
\\
The collective storage required to store the textual content of 5 billion web pages is:\\
\protect\phantomsection\label{katex_inline_by8i1fNAc8fYpcCP4IWNW}{{{\(\text{Total storage per crawl} = 5\ \text{Billion} \times (2070\ \text{KB} + 500\ \text{B}) = 10.35\ \text{PB}\)}}}

\begin{figure}[htbp]
 \centering
 \includegraphics[width=0.5\textwidth]{Images/chapter_35/section_4695113376989184/5122240813596672.png}
 \caption{The total storage required by the web crawler system}
\end{figure}

\subsubsection{Traversal time}\label{oIMaOsKksG4ApdDnhjClG}
\\
Since the traversal time is just as important as the storage requirements, let's calculate the approximate time for one-time crawling. Assuming that the average HTTP traversal per webpage is 60 ms(Webpage traversal is a function of the page size. Smaller web pages take less than 60 ms, and large pages take longer; however, the number 60 ms is the average traversal time.), the time to traverse all 5 billion pages will be:
\\
\protect\phantomsection\label{katex_inline_6Pmoa7mDN-ASYAIh6b_dX}{{{\(\text{Total traversal time} = 5\ \text{Billion} \times 60\ \text{ms} = 0.3\ \text{Billion seconds} = \text{9.5 years}\)}}}
\\
It'll take approximately 9.5 years to traverse the whole Internet while using one instance of crawling, but we want to achieve our goal in one day. We can accomplish this by designing our system to support multi-worker architecture and divide the tasks among multiple workers running on different servers.

\subsubsection{Number of servers estimation for multi-worker architecture}\label{Ha6fpM3ax9OiygP-CRCSl}
\\
Let's calculate the number of servers required to finish crawling in one day. Assume that there is only one worker per server.
\\
\protect\phantomsection\label{katex_inline_e3djLhi70WecZhmn3RgcC}{{{\(\text{No. of days required by 1 server to complete the task} = 9.5\ \text{years} \times 365\ \text{days} \approx 3468\ \text{days}\)}}}
\\
One server takes 3,468 days to complete the task.
\\
How many servers would we need to complete this same task in one day?
\\
We would need 3,468 servers to complete the same task in just one day.

\begin{figure}[htbp]
 \centering
 \includegraphics[width=0.25\textwidth]{Images/chapter_35/section_4695113376989184/4504835573809152.png}
 \caption{The number of servers required for the web crawler system}
\end{figure}

If there are a \(n\) number of threads per server, we'll divide 3,468 by \(n\). For example, if one server is capable of executing ten threads at a time, then the number of servers is reduced to \(\frac{3468}{10}\approx 347 servers\)

\subsubsection{Bandwidth estimation}\label{HAL58N62PHHAzn1sTZ_jI}
\\
Since we want to process 10.35PB of data per day the total bandwidth required would be:\\
\strut \\
\protect\phantomsection\label{katex_inline_oj8HZm356d79WVrCjxiAS}{{{\(\frac{10.35\ \text{PB}}{86400\ \text{sec}} \approx 120\ \text{GB/sec} \approx 960\ \text{Gb/sec}\)}}}\\
\strut \\
\protect\phantomsection\label{katex_inline_JkTR9ZA2THsRL6e7bgvuN}{{{\(960 \text{ Gb/sec}\)}}} is the total required bandwidth. Now, assume that the task is distributed equally among \protect\phantomsection\label{katex_inline_uma7-SWaYafwVXAly141Y}{{{\(3468 \ \text{servers}\)}}} to accomplish the task in one day. Thus, the per server bandwidth would be:\\
\strut \\
\protect\phantomsection\label{katex_inline_VzgYBEN2Hevd29eJ-YGYf}{{{\(\frac{960\ \text{Gb/sec}}{3468\ \text{servers}} \approx 277\ \text{Mb/sec per server}\)}}}

\begin{figure}[htbp]
 \centering
 \includegraphics[width=0.5\textwidth]{Images/chapter_35/section_4695113376989184/5921107918323712.png}
 \caption{The total bandwidth required for the web crawler system}
\end{figure}

Let\textquotesingle s play around with the initial assumptions and see how the estimates change in the following calculator:

These estimations give us a tangible sense of the system\textquotesingle s size. The next logical step is to select the high-level tools capable of handling this scale.

\subsection{Building blocks we will use}\label{SEPWNZUpJcarC7AvnSiDS}
\\
Here is the list of the main building blocks we'll use in our design:

\begin{figure}[htbp]
 \centering
 \includegraphics[width=0.5\textwidth]{Images/chapter_35/section_4695113376989184/6488241941643264.png}
 \caption{Building blocks in high-level design}
\end{figure}

\begin{itemize}
\item
\protect\phantomsection\label{ago152uhpWP0sJ7QfZBhA}
\href{https://www.educative.io/courses/grokking-the-system-design-interview/system-design-the-distributed-task-scheduler}{\textbf{Scheduler}} is used to schedule crawling events on the URLs that are stored in its database.
\item
\protect\phantomsection\label{EFZYmlBrFIE5fTtFQKlEs}
\href{https://www.educative.io/courses/grokking-the-system-design-interview/introduction-to-domain-name-system-dns}{\textbf{DNS}} is needed to get the IP address resolution of the web pages.
\item
\protect\phantomsection\label{7Hx3kic5wWqIJwmBK30He}
\href{https://www.educative.io/courses/grokking-the-system-design-interview/system-design-the-distributed-cache}{\textbf{Cache}} is utilized in storing fetched documents for quick access by all the processing modules.
\item
\protect\phantomsection\label{emObIh0hC0bPGW5J5V6rU}
\href{https://www.educative.io/courses/grokking-the-system-design-interview/system-design-a-blob-store}{\textbf{Blob store}}'s main application is to store the crawled content.
\end{itemize}
\\
Besides these basic building blocks, our design includes some additional components as well:

\begin{itemize}
\item
\protect\phantomsection\label{SF9syPVBKoKTo4glu8qvM}
The \textbf{HTML fetcher} establishes a network communication connection between the crawler and the web hosts.
\item
\protect\phantomsection\label{wbUwUkxktaBJohTQ5KG2A}
The \textbf{service host} manages the crawling operation among the workers.
\item
\protect\phantomsection\label{7i14WSSmSu42p4KgPS6RX}
The \textbf{extractor} extracts the embedded URLs and the document from the web page.
\item
\protect\phantomsection\label{MRFttqqQ0lFARBibKJ_SV}
The \textbf{duplicate eliminator} performs dedup testing on the incoming URLs and the documents.
\end{itemize}

\begin{figure}[htbp]
 \centering
 \includegraphics[width=0.25\textwidth]{Images/chapter_35/section_4695113376989184/6706967778033664.png}
 \caption{The components in a high-level design}
\end{figure}

The following section will explain how these components are orchestrated to meet our system requirements.

\subsection{Design}\label{CwVF2VcH5h8RfEnzAZcTX}
\\
This section describes additional components involved in the design and workflow of the web crawling process with respect to its requirements.

\subsubsection{Components}\label{EZhYw-7IEf9XOWLWPEiSc}
\\
Here are the details of the building blocks and the components needed for our design:

\begin{itemize}
\item
\protect\phantomsection\label{_27rvSHgSqJ4X1SF8EUu5}
\textbf{Scheduler}: This is one of the key building blocks that schedules URLs for crawling. It's composed of two units: a priority queue and a relational database.
\end{itemize}

\begin{enumerate}
\item
\begin{enumerate}
\item
\protect\phantomsection\label{BkR5XN5t8GKDkHVcymGXR}
\textbf{A priority queue (URL frontier)}: The queue hosts URLs that are made ready for crawling based on the two properties associated with each entry: priority(As a requirement, we need to assign variable priorities to URLs, depending on their content. This attribute defines the precedence of a URL while in the URL frontier.) and updates frequency(For recrawling purposes, we need to define the recrawl frequency for each URL. This attribute ensures a defined number of placements in the URL frontier for each URL.).
\item
\protect\phantomsection\label{qR6PSQIJe7liqHLu-v1AA}
\textbf{Relational database}: It stores all the URLs along with the two associated parameters mentioned above. The database gets populated by new requests from the following two input streams:
\end{enumerate}
\end{enumerate}

\begin{itemize}
\item
\begin{itemize}
\item
\protect\phantomsection\label{x4W1ZggWClJsHVIF50H6H}
The user's \emph{added URLs}, which include seed and runtime added URLs.
\item
\protect\phantomsection\label{UM30MyPVxKrU6oEuj-Srz}
The crawler's \emph{extracted URLs}.
\end{itemize}
\end{itemize}

\begin{quote}
\textbf{Quiz:}
\textbf{Question 1:} Can we estimate the size of the priority queue? What are the pros and cons of a centralized and distributed priority queue.
\textbf{Answer:} Assumption: Let\textquotesingle s assume that at any point, we have roughly one million URLs in the URL frontier to be crawled upon. Let's calculate the size of the priority queue needed to store all of these

URLs. \[
\text{Size of priority queue}
= 1\,\text{million URLs} \times 2048\,\text{Bytes}
= 2.048\,\text{GB}
\]
 2.048 GB is a reasonable amount of space for a queue, indicating that we might not need to implement a distributed mechanism for the URL frontier.
However, a centralized queue has limited read/write bandwidth and is a single point of failure. Therefore, having a sub-queue for each worker will be the best approach.
If we use our distributed queue, all workers can get data out of the same queue if they want. But having independent queues can further optimize the crawling process, especially in the case of high priority and more frequent crawls like those news websites that need more than regular workers to do the frequent crawling. This approach will inherently facilitate the case of increasing queue size. Having a single queue is beneficial for the deduplication of redundant links and better for the overall crawler resources. We'll handle the recrawling priority and frequency another way, as explained in the next sections, for which we need the distribution mechanism.
\textbf{Question 2:} How will we distribute the URL frontier among different workers and what purpose will this serve?
\textbf{Answer:} As was defined earlier, the URL frontier is a priority queue used in the scheduler and it holds the URLs that need to be crawled through. When we talk about its distribution, we mean taking the hash value of the hostname\textquotesingle s URLs and mapping them to specific workers.

This way, each worker will have its own sub-queue. This fulfills the following two requirements of our system: - The workers don't individually connect to more than one host web servers at a time. - The workers don't overburden the host web servers with concurrent requests because we use FIFO sub-queues.
\end{quote}

\begin{itemize}
\item
\protect\phantomsection\label{wUyhEGCSbTqDMb6K_6MdP}
\textbf{DNS resolver}: The web crawler needs a DNS resolver to map hostnames to IP addresses for HTML content fetching. Since DNS lookup is a time-consuming process, a better approach is to create a customized DNS resolver and cache frequently-used IP addresses within their time-to-live because they're bound to change after their time-to-live.
\item
\protect\phantomsection\label{BacsK7GFPuuvYJ-zNlQGM}
\textbf{HTML fetcher}: The HTML fetcher initiates communication with the server that's hosting the URL(s). It downloads the file content based on the underlying communication protocol. We focus mainly on the \emph{HTTP protocol} for textual content, but the \emph{HTML fetcher} is easily extendable to other communication protocols, as is mentioned in the section on the non-functional requirements of the web crawler.
\end{itemize}

\begin{quote}
\textbf{Quiz:}
\textbf{Question 1:} How does the crawler handle URLs with variable priorities?
\textbf{Answer:} The crawler has to be vigilant enough at each stage to differentiate between various priority levels of URLs. Let's see how the crawler design handles such cases stage by stage: 1. Since we implement our URL

frontier as a priority queue of a scheduler, it automatically handles the placement based on parameter values. We have chosen the fault tolerance and periodicity parameters as priority indicators for our URLs. The assignment of these parameters depends on the nature of the web pages' content. If it is a news web page, crawling through it multiple times in a day is appropriate and required for our index to be up-to-date. Any ordinary web page with occasional updates happening might have a standard visit frequency of, let's say, two weeks. 2.
Similarly, at the HTML-fetcher level where the crawler is communicating with the host server based on the `robots.txt` guidelines, it can communicate the concerned parameter's value for the fetched URLs back to the scheduler in the storing phase. Instead of having indicators associated with the URLs, another solution can be to have separate queues for different priorities. We can then dequeue from these queues based on the priorities assigned to them. This approach just requires the URL placement in the respective queue and doesn't need scripts to schedule based on the extra parameters associated. It all depends on the scale of our crawling application.
\end{quote}

\begin{itemize}
\item
\protect\phantomsection\label{r0u6O97J5l82Z7eRqL6L-}
\textbf{Service host:} This component acts as the brain of the crawler and is composed of worker instances. For simplicity, we will refer to this whole component or a single worker as a crawler. There are three main tasks that this service host/crawler performs:
\end{itemize}

\begin{enumerate}
\item
\begin{enumerate}
\item
\protect\phantomsection\label{-ZsVq2z968M1LWKPMxSLs}
It handles the multi-worker architecture of the crawling operation. Based on the availability, each worker communicates with the URL frontier to dequeue the next available URL for crawling.
\item
\protect\phantomsection\label{QpXoKg2XqAiWRtUaIQ2Z8}
Each worker is responsible for acquiring the DNS resolutions of the incoming URLs from the DNS resolver.
\item
\protect\phantomsection\label{ESD6hIM5jlTuVSgsveBE9}
Each worker acts as a gateway between the scheduler and the HTML fetcher by sending the necessary DNS resolution information to the HTML fetcher for communication initiation.
\end{enumerate}
\end{enumerate}

\begin{figure}[htbp]
 \centering
 \includegraphics[width=0.5\textwidth]{Images/chapter_35/section_4695113376989184/5738816695828480.png}
 \caption{Service host interaction with other components}
\end{figure}

\begin{itemize}
\item
\protect\phantomsection\label{nTNucDHN1U8eWKaC0Br7r}
\textbf{Extractor}: Once the HTML fetcher gets the web page, the next step is to extract two things from the webpage: URLs and the content. The extractor sends the extracted URLs directly and the content with the \textbf{document input stream (DIS)} to the duplicate eliminator. DIS is a cache that's used to store the extracted document, so that other components can access and process it. Over here, we can use Redis as our cache choice because of its advanced data structure functionality.\\
Once it's verified that the duplicates are absent in the data stores, the extractor sends the URLs to the task scheduler that contains the URL frontier and stores the content in blob storage for indexing purposes.
\item
\protect\phantomsection\label{Wk6aS9ipfGpBX7URze3L-}
\textbf{Duplicate eliminator}: Since the web is all interconnected, the probability of two different URLs referring to the same web page or different URLs referring to various web pages having the same content is evident. The crawler needs a component to perform a dedup test to eliminate the risk of exploiting resources by storing and processing the same content twice. The duplicate eliminator calculates the checksum value of each extracted URL and compares it against the URLs checksum data store. If found, it discards the extracted URL. Otherwise, it adds a new entry to the database with the calculated checksum value.
\end{itemize}

\begin{figure}[htbp]
 \centering
 \includegraphics[width=0.5\textwidth]{Images/chapter_35/section_4695113376989184/5713984134447104.png}
 \caption{Dedup testing in action}
\end{figure}

The duplicate eliminator repeats the same process with the extracted content and adds the new webpage's checksum value in the document checksum data store for future matchings.
\\
Our proposed design for the duplicate eliminator can be made robust against these two issues:

\begin{enumerate}
\item
\protect\phantomsection\label{vOCBPfys_8gbEa3gnpdKD}
By using URL redirection, the new URL can pass through the URL dedup test. But , the second stage of the document dedup wouldn't allow content duplication in the blob storage.
\item
\protect\phantomsection\label{Zj8wUzJBFdNCpV-L54jjs}
By changing just one Byte in a document, the checksum of the modified document is going to come out different than the original one.
\end{enumerate}

\begin{itemize}
\item
\protect\phantomsection\label{ZXN-JdwKt3dP5Y6tIFRoU}
\textbf{Blob store}: Since a web crawler is the backbone of a search engine, storing and indexing the fetched content and relevant metadata is immensely important. The design needs to have a distributed storage, such as a blob store, because we need to store large volumes of unstructured data.
\end{itemize}
\\
The following illustration shows the pictorial representation of the overall web crawler design:

\begin{figure}[htbp]
 \centering
 \includegraphics[width=0.5\textwidth]{Images/chapter_35/section_4695113376989184/5938686891261952.png}
 \caption{The overall web crawler design}
\end{figure}

\subsubsection{Workflow}\label{BfDaCBIJk8O2YcBEhoxlB}

\begin{enumerate}
\item
\protect\phantomsection\label{5BampHcqkS9W3qrJQHCyM}
\textbf{Assignment to a worker}: The crawler (service host) initiates the process by loading a URL from the URL frontier's priority queue and assigns it to the available worker.
\item
\protect\phantomsection\label{HDhzddSvC7VA0j0UKLeeB}
\textbf{DNS resolution}: The worker sends the incoming URL for DNS resolution. Before resolving the URL, the DNS resolver checks the cache and returns the requested IP address if it's found. Otherwise, it determines the IP address, sends it back to the worker instance of the crawler, and stores the result in the cache.
\end{enumerate}

The implementation of the URL frontier---dequeuing the URLs from a FIFO priority queue and enqueuing all the extracted URLs back into the queue rather than crawling them one after another---ensures that we are crawling the web in a breadth-first search (BFS) rather than a depth-first-search (DFS) manner.

\begin{quote}
\textbf{Quiz:}
\textbf{Question 1:} Can we use DFS instead of BFS?
\textbf{Answer:} We can use DFS when we want to utilize a website's persistent connection to traverse all the web pages on that specific domain. This saves time as it helps us avoid reconnecting with the same website repeatedly in case the session expires.
\end{quote}

\begin{enumerate}
\setcounter{enumi}{2}
\item
\protect\phantomsection\label{W5_6h6i4sy_yqcXiIfSvX}
\textbf{Communication initiation by the HTML fetcher}: The worker forwards the URL and the associated IP address to the HTML fetcher, which initiates the communication between the crawler and the host server.
\item
\protect\phantomsection\label{MxdcjPwcngSSJAtQ-Er_h}
\textbf{Content extraction}: Once the worker establishes the communication, it extracts the URLs and the HTML document from the web page and places the document in a cache for other components to process it.
\item
\protect\phantomsection\label{HOS1-V44iL1G_-yGhWY1m}
\textbf{Dedup testing}: The worker sends the extracted URLs and the document for dedup testing to the duplicate eliminator. The duplicate eliminator calculates and compares the checksum of both the URL and the document with the checksum values that have already been stored. The duplicate eliminator discards the incoming request in case of a match. If there's no match, it places the newly-calculated checksum values in the respective data stores and gives the go-ahead to the extractor to store the content.
\item
\protect\phantomsection\label{vbCbAr-fpS8yemEyIqIxF}
\textbf{Content storing}: The extractor sends the newly-discovered URLs to the scheduler, which stores them in the database and sets the values for the priority and recrawl frequency variables. The extractor also writes the required portions of the newly discovered document---currently in the DIS---into the database.
\item
\protect\phantomsection\label{17IjrFDi97ujJP9phz6Jd}
\textbf{Recrawling}: Once a cycle is complete, the crawler goes back to the first point and repeats the same process until the URL frontier queue is empty.\\
The URLs stored in the scheduler's database have priority and periodicity assigned to them. Enqueuing new URLs into the URL frontier depends on these two factors.
\end{enumerate}
\begin{quote}
\textbf{Note:} Because of multiple instances of each service and microservices architecture, our design can make use of client-side load balancing (see \href{https://www.educative.io/courses/grokking-the-system-design-interview/client-side-load-balancer-for-twitter}{Client-side Load Balancer for Twitter}).
\end{quote}
\\
The following slideshow gives a detailed overview of the web crawler workflow:

\begin{figure}[htbp]
 \centering
 \begin{subfigure}[b]{0.48\textwidth}
 \centering
 \includegraphics[width=\textwidth]{Images/chapter_35/section_4695113376989184/4718936060198912.png}
 \caption{Assignment to a worker}
 \end{subfigure}
 \hfill
 \begin{subfigure}[b]{0.48\textwidth}
 \centering
 \includegraphics[width=\textwidth]{Images/chapter_35/section_4695113376989184/6616942825046016.png}
 \caption{DNS resolution}
 \end{subfigure}
\end{figure}

\begin{figure}[htbp]
 \centering
 \begin{subfigure}[b]{0.48\textwidth}
 \centering
 \includegraphics[width=\textwidth]{Images/chapter_35/section_4695113376989184/4622010107166720.png}
 \caption{HTTP connection initiation}
 \end{subfigure}
 \hfill
 \begin{subfigure}[b]{0.48\textwidth}
 \centering
 \includegraphics[width=\textwidth]{Images/chapter_35/section_4695113376989184/5356443542093824.png}
 \caption{Content extraction}
 \end{subfigure}
\end{figure}

\begin{figure}[htbp]
 \centering
 \begin{subfigure}[b]{0.48\textwidth}
 \centering
 \includegraphics[width=\textwidth]{Images/chapter_35/section_4695113376989184/6247547971960832.png}
 \caption{Dedup testing}
 \end{subfigure}
 \hfill
 \begin{subfigure}[b]{0.48\textwidth}
 \centering
 \includegraphics[width=\textwidth]{Images/chapter_35/section_4695113376989184/6248216778899456.png}
 \caption{Content storing}
 \end{subfigure}
\end{figure}

\begin{figure}[htbp]
 \centering
 \begin{subfigure}[b]{0.48\textwidth}
 \centering
 \includegraphics[width=\textwidth]{Images/chapter_35/section_4695113376989184/5339519022137344.png}
 \caption{Recrawling}
 \end{subfigure}
\end{figure}

Recrawling is the process of revisiting and updating indexed pages. Keeping this in mind, try to answer the following questions:

\begin{enumerate}
\item
Why do we need to recrawl?
\item
How do we decide when to recrawl?
\end{enumerate}

\textbf{Question:}

Why and when do we need to recrawl?

\textbf{Reference Answer:}

Recrawling is essential to ensure the freshness of indexed pages, reflecting updated content on websites. The decision to re-crawl is based on factors such as content frequency, relevance, and the importance of timely information. Regular recrawling is determined by changes in content, enabling search engines to provide users with the latest and most relevant results.

The next sections provide a detailed overview of the improvements required to enhance the functionality, performance, and security of our web crawler design.

\subsection{Design improvements}\label{kedmMqGrBHlFuvQeg9EoQ}
\\
Our current design is simplistic and has some inherent shortcomings and challenges. Let
\textquotesingle s highlight them one by one and make some adjustments to our design along the way.

\begin{itemize}
\item
\protect\phantomsection\label{A5Y0XCmJV_oP2DyYwi0ql}
\textbf{Shortcoming:} Currently, our design supports the HTTP protocol and only extracts textual content. This leads to the question of how we can extend our crawler to facilitate multiple communication protocols and extract various types of files.\\
\textbf{Adjustment:} Since we have two separate components for serving communication handling and extracting, HTML Fetcher and Extractor, let\textquotesingle s discuss their modifications one by one.\\

\begin{itemize}
\item
\textbf{HTML Fetcher:} We have only discussed the HTTP module in this component so far because of the widely-used HTTP URLs scheme. We can easily extend our design to incorporate other communication protocols like File Transfer Protocol (FTP). The workflow will then have an intermediary step where the crawler invokes the concerned communication module based on the URL's scheme. The subsequent steps will remain the same.
\item
\textbf{Extractor:} Currently, we only extract the textual content from the downloaded document placed in the Document Input Stream (DIS). This document contains other file types as well, for example, images and videos. If we wish to extract other content from the stored document, we need to add new modules with functionalities to process those media types. Since we use a blob store for content storage, storing the newly-extracted content comprising text, images, and videos won\textquotesingle t be a problem.
\end{itemize}
\end{itemize}

\begin{figure}[htbp]
 \centering
 \includegraphics[width=0.5\textwidth]{Images/chapter_35/section_4695113376989184/6678307800481792.png}
 \caption{The extensibility of the HTML fetcher and extractor}
\end{figure}

\begin{itemize}
\item
\protect\phantomsection\label{dx5f30CEIJOzYG7tOly1q}
\textbf{Shortcoming:} The current design doesn\textquotesingle t explain how the multi-worker concept integrates into this system\\
\textbf{Adjustment:} Every worker needs a URL to work on from the priority queue. Different websites require different times for workers to finish crawling, so each worker will dequeue a new URL upon its availability.\\
There are several ways to achieve this multi-worker architecture for our system. Some of them are as follows:

\begin{itemize}
\item
\textbf{Domain level log:} The crawler assigns one whole domain to a worker. All the URLs branching out of the initial URL are the responsibility of the same crawler. We ensure this by caching the hash of the hostname against the worker ID for guaranteed future assignment to the same worker. This also helps us avoid any redundant crawling of that domain's web pages by any worker.\\
This approach is best-suited for achieving \textbf{reverse URL indexing}, which involves traversing the web address directories in a reversed order. It ensures the storage efficiency of the URL storing process for later use and prevents extensive repetitive string matching processes for duplication testing.
\item
\textbf{Range division:} The range of URLs given to each crawler; the crawler distributes a range of URLs from the priority queue among workers to avoid clashes. Like the previous approach, the crawler must hash the range associated with each worker.
\item
\textbf{Per URL crawling:} Queue all the URLs; a worker takes a URL and enqueues the subsequently found URLs into the priority queue. Those new URLs are readily available to crawl through for other workers. To avoid enqueuing multiple similar links that direct to the same web page, we calculate the checksum of the canonicalized URL.
\end{itemize}
\end{itemize}

\subsection{Crawler traps}\label{h2T5azDMjfQifa9Scn3AR}
\\
A \textbf{crawler trap} is a URL or a set of URLs that cause indefinite crawler resource exhaustion. This section dedicates itself to the classification, identification, and prevention of crawler traps.

\subsubsection{Classification}\label{dMVGoovKqIW9qAgDqjgYL}
\\
There can be many classification schemes for crawler traps, but let's classify them based on the URL scheme.
\\
Mostly, web crawler traps are the result of poor website structuring, such as:

\begin{itemize}
\item
\protect\phantomsection\label{kdmEbZbOIEjRmfGaksNGr}
\textbf{URLs with query parameters}: These query parameters can hold an immense amount of values, all while generating a large number of useless web pages for a single domain: \texttt{HTTP://www.abc.com?query}.
\item
\protect\phantomsection\label{l_5y3I7lE-VY1S28Zk5DW}
\textbf{URLs with internal links}: These links redirect in the same domain and can create an infinite cycle of redirection between the web pages of a single domain, making the crawler crawl the same content over and over again.
\item
\protect\phantomsection\label{fEXBgLBVobA2-RVzU2tCQ}
\textbf{URLs with infinite calendar pages}: These have never-ending combinations of web pages based on the varying date values, and can create a large number of pointless web pages for a single domain.
\item
\protect\phantomsection\label{_Ims2wrzQOK-tYry8Ikpc}
\textbf{URLs for the dynamic content generation}: These are query-based and can generate a massive number of web pages based on the dynamic content resulting from these queries. Such URLs might become a never-ending crawl on a single domain.
\item
\protect\phantomsection\label{oTUDYCwEQGsMPRZ1nep1M}
\textbf{URLs with repeated/cyclic directories}: They form an indefinite loop of redirections. For example, \texttt{HTTP://www.abc.com/first/second/first/second/...}.
\end{itemize}

\begin{figure}[htbp]
 \centering
 \includegraphics[width=0.5\textwidth]{Images/chapter_35/section_4695113376989184/4725894728122368.png}
 \caption{Classification of web crawler traps}
\end{figure}

Mostly, the crawler traps are unintended because of the poor structuring of the website. Interestingly , crawler traps can also be placed intentionally to dynamically generate an endless series of web pages with the pure purpose of exhausting a crawler's bandwidth. These traps severely impact the throughput(useful content crawled in a time period) of a crawler and hamper its performance.
\\
These crawler traps might be detrimental to the crawler, but they also seriously affect the website's SEO ranking.

\subsubsection{Identification}\label{JZ9RdiMaWDADPY292M7b-}
\\
It's essential to categorize the crawling traps to correctly identify them and think of design adjustments accordingly.
\\
There are mainly two layers to the process of identifying a crawler trap:

\begin{enumerate}
\item
\protect\phantomsection\label{ytrLI-wKa_DJbVSBly3f-}
\textbf{Analyzing the URL scheme}: The URLs with a poor URL structure, for example, those with cyclic directories: \texttt{HTTP://www.abc.com/first/second/first/second/...} will create crawler traps. Hence, filtering such URLs beforehand will rescue our crawler resources.
\item
\protect\phantomsection\label{0D6PArX12-bT_a6AZD-zW}
\textbf{Analyzing the total number of web pages against a domain}: An impossibly large number of web pages against a domain in the URL frontier is a strong indicator of a crawler trap. So, limiting the crawl at such a domain will be of utmost importance.
\end{enumerate}

\begin{figure}[htbp]
 \centering
 \includegraphics[width=0.5\textwidth]{Images/chapter_35/section_4695113376989184/6641571349135360.png}
 \caption{The identification process of web crawler traps}
\end{figure}

\subsubsection{Solution}\label{cK9lw3da7Orxs7mWB_Znp}
\\
Our design lacks details on the responsible crawling mechanism to avoid crawler traps for the identifications described above.
\\
Crawling is a resource and time-consuming task, and it's of extreme importance to efficiently avoid crawler traps in order to achieve timely and helpful crawling. Once the crawler starts communicating with the server for downloading the web page content, there are multiple factors that it needs to take into consideration, mainly at the HTML-fetcher level. Let 's go over these factors individually.

\begin{enumerate}
\item
\protect\phantomsection\label{4-4rtofIuZ4WCtIACCa2B}
The crawler must implement an application-layer logic to counter the crawler traps. This logic might be based on the observed number of web pages exceeding a specified threshold.\\
The crawler must be intelligent enough to limit its crawling at a specific domain after a finite amount of time or web page visits. A crawler should smartly exit a web page and store that URL as a no-go area for future traversals to ensure performance effectiveness.
\item
\protect\phantomsection\label{kjc8_9mZhIiagEjwhOKmK}
When initiating communication with the web server, the crawler needs to fetch a file named \texttt{robots.txt}. This file contains the dos and don'ts for a crawler listed by the web masters. Adhering to this document is an integral part of the crawling process. It also allows the crawler to access certain domain-specified web pages prioritized for crawling, without limiting its access to specific web pages.\\
Another essential component of this document is the revisit frequency instructions for the crawler. A popular website might demand a frequent revisit, contrary to a website that rarely updates its content. This standard of websites communicating with the web crawlers is called the \textbf{Robots Exclusion Protocol}. This protocol prevents the crawlers from unnecessarily spending crawling resources on uncrawlable web pages.
\end{enumerate}

\begin{figure}[htbp]
 \centering
 \includegraphics[width=0.5\textwidth]{Images/chapter_35/section_4695113376989184/4686790988529664.png}
 \caption{The robots exclusion protocol}
\end{figure}

\begin{quote}
The \texttt{robots.txt} file doesn't protect crawlers from malicious or intended crawler traps. The other explained mechanisms must handle those traps.
\end{quote}
\begin{enumerate}
\setcounter{enumi}{2}
\item
\protect\phantomsection\label{S0TPUJ6fABRridyCdlSdK}
Since each domain has a limited incoming and outgoing bandwidth allocation, the crawler needs to be polite enough to limit its crawling at a specific domain. Instead of having a static crawl speed for every domain, a better approach is to adjust the crawl speed based on a domain's \textbf{Time to First Byte} (\textbf{TTFB}) value. The higher the TTFB value, the slower the server. And so, crawling that domain too fast might lead to more time-out requests and incomplete crawling.
\end{enumerate}
\\
These modifications in the design will ensure a crawler capable of avoiding crawler traps and hence optimizing resource usage. After refining our design to handle real-world challenges, the final step is to validate our complete architecture.

\subsection{Requirements compliance}\label{a2ibNrlpABbYpgGK1fvB_}
\\
Let's evaluate how our design meets the non-functional requirements of the proposed system.

\subsubsection{Scalability}\label{1E8Qv4BYYGBsKwOLqOjsL}
\\
Our design states that scaling our system horizontally is vital. Therefore, the proposed design incorporates the following design choices to meet the scalability requirements:

\begin{itemize}
\item
\protect\phantomsection\label{RpxrhVQxhsBiJ7IgDAA4w}
The system is scalable to handle the ever-increasing number of URLs. It includes all the required resources, including schedulers, web crawler workers, HTML fetchers, extractors, and blob stores, which are added/removed on demand.
\item
\protect\phantomsection\label{XHUAtwb0C3T4mpptmlZsR}
In the case of a distributed URL frontier, the system utilizes consistent hashing to distribute the hostnames among various crawling workers, where each worker is running on a server. With this, adding or removing a crawler server isn't a problem.
\end{itemize}

\subsubsection{Extensibility and modularity}\label{kEFWwib7KO7SFitlw9Pk3}
\\
So far, our design is only focusing on a particular type of communication protocol: HTTP. But according to our non-functional requirements, our system's design should facilitate the inclusion of other network communication protocols like FTP.
\\
To achieve this extensibility, we only need to add additional modules for the newly required communication protocols in the HTML fetcher. The respective modules will then be responsible for making and maintaining the required communications with the host servers.
\\
Along the same lines, we expect our design to extend its functionality for other MIME(A multipurpose Internet mail extension, or MIME type, is an Internet standard that describes the contents of Internet files based on their natures and formats.) types as well. The modular approach for different MIME schemes facilitates this requirement. The worker will call the associated MIME's processing module to extract the content from the document stored in the DIS.

\begin{figure}[htbp]
 \centering
 \includegraphics[width=0.25\textwidth]{Images/chapter_35/section_4695113376989184/6746063834972160.png}
 \caption{The requirements met by our web crawler}
\end{figure}

\subsubsection{Consistency}\label{pe0bkNLEReIGKdEcMzhh8}
\\
Our system consists of several crawling workers. Data consistency among crawled content is crucial. So , to avoid data inconsistency and crawl duplication, our system computes the checksums of URLs and documents and compares them with the existing checksums of the URLs and documents in the \emph{URL} and \emph{document checksum} data stores, respectively.
\\
Apart from deduplication, to ensure the data consistency by fault-tolerance conditions, all the servers can checkpoint their states to a backup service, such as Amazon S3 or an offline disk, regularly.

\subsubsection{Performance}\label{QBDzTia-BYNgn69BEo2Nr}
\\
Our web crawler's performance depends on the following factors:

\begin{itemize}
\item
\protect\phantomsection\label{4jwc_40D6Kkr4EzZ9_v8H}
\textbf{URLs crawled per second}: We can improve this factor by adding new workers to the system.
\item
\protect\phantomsection\label{f3j1QOS9CjOogT0lLbNFU}
\textbf{Utilizing blob storage for content storing}: This ensures higher throughput for the massive amount of unstructured data. It also indicates a fast retrieval of the stored content, because a single blob can support up to 500 requests per second.
\item
\protect\phantomsection\label{Zp16oaxW8G5qMZH5y7VUb}
\textbf{Efficient implementation of the} \textbf{\texttt{robots.txt}} \textbf{file guideline}: We can implement this performance factor by having an application-layer logic of setting the highest precedence of \texttt{robots.txt} guidelines while crawling.
\item
\protect\phantomsection\label{RdtyJt3CbuwfvueTkwVy4}
\textbf{Self-throttling}: We can have various application-level checks to ensure that our web crawler doesn't hamper the performance of the website host servers by exhausting their resources.
\end{itemize}

\subsubsection{Scheduling}\label{_J6o9Z4YAi3EXeMS4trE_}
\\
As was established previously, we may need to recrawl URLs on various frequencies. These frequencies are determined by the application of the URL. We can determine the frequency of recrawl in two different ways:

\begin{enumerate}
\item
\protect\phantomsection\label{HBo6qfSVXObYksiwLB1gs}
We can assign a default or a specific recrawling frequency to each URL. This assignment depends on the application of the URL defining the priority. A default frequency is assigned to the standard-priority URLs and a higher recrawl frequency is given to the higher-priority URLs. Based on each URL's associated recrawl frequency, we can decide to enqueue URLs in the priority queue from the scheduler's database. The priority defines the place of a URL in the queue.
\item
\protect\phantomsection\label{OT0_mK773UuZJj-ZIa0MD}
The second method is to have separate queues for various priority URLs, use URLs from high-priority queues first, and subsequently move to the lower-priority URLs.
\end{enumerate}
\\
The following table summarizes the techniques for achieving non-functional requirements in a web crawler system:

\textbf{Non-functional Requirements Compliance}

\begin{table}[htbp]
\centering
\small
\adjustbox{max width=\textwidth}{
\begin{tabular}{|p{0.274\textwidth}|p{0.676\textwidth}|}
\hline
\textbf{Requirement} & \textbf{Techniques} \\
\hline
\hline
Scalability & \begin{itemize} \tightlist \item Addition/removal of different servers based on the increase/decrease in load \item Consistent hashing to manage server's addition and removal \item Regular backup of the servers in Amazon S3 backup service to achieve fault tolerance \end{itemize} \\
\hline
Extensibility and Modularity & \begin{itemize} \tightlist \item Addition of a newer communication protocol module in the HTML fetcher \item Addition of new MIME schemes while processing the downloaded document in DIS \end{itemize} \\
\hline
Consistency & \begin{itemize} \tightlist \item Calculation and comparison of checksums of URLs and Documents in the respective data stores \end{itemize} \\
\hline
Performance & \begin{itemize} \tightlist \item Increasing the number of workers performing the crawl \item Blob stores for storing the content \item High priority to robots.txt file guidelines while crawling \item Self-throttle at a domain while crawling \end{itemize} \\
\hline
Scheduling & \begin{itemize} \tightlist \item Pre-defined default recrawl frequency, or \item Separate queues and their associated frequencies for various priority URLs \end{itemize} \\
\hline
\end{tabular}
}
\caption{Non-functional Requirements Compliance}
\end{table}

Consider a real-time news aggregation platform, where content changes rapidly, and frequent recrawls are crucial for up-to-date information. What immediate steps would you take if the \textbf{IP} faces blocking issues during frequent crawls? Share strategies for handling IP blocks to ensure continuous access to the latest content.

\textbf{Question:}

Share strategies for handling IP blocks

\textbf{Reference Answer:}

Immediate actions involve implementing IP rotation, which entails regularly changing the IP address making requests. This helps bypass the block as it presents a different identifier to the server. Concurrently , utilizing proxy servers becomes crucial. Proxies act as intermediaries between the crawler and the target server, enabling distributed requests from various IP addresses, enhancing resilience against blocks.
\\
Furthermore, continuous monitoring and adaptability to changes in blocking mechanisms are imperative. Regularly assess the effectiveness of IP rotation and proxy usage, modifying strategies as needed to counter evolving blocking tactics.
\\
To fortify against potential blocks, consider employing anti-blocking measures. These may involve introducing delays between requests, mimicking more natural user behavior, or incorporating advanced techniques to obfuscate the crawler's footprint.

\subsection{Conclusion}\label{numadAThKIOpX2rqvGgfK}
\\
The web crawler system entails a multi-worker design that uses a microservices architecture. Besides achieving the basic crawling functionality, our design provides insights into the potential shortcomings and challenges associated with our design and further rectifies them with appropriate design modifications. The noteworthy features of our design are as follows:

\begin{enumerate}
\item
\protect\phantomsection\label{2qsbYSN-WcXcm4jZGxO2k}
Identification and design modification for crawler traps
\item
\protect\phantomsection\label{haQWv_83wNwYrRFZzYEHK}
Extensibility of HTML fetching and content extraction modules
\end{enumerate}

% End of section content